using System;

namespace EngineeringCalculator {
    class Program {
        static void Main() {
            CalculatorMenu menu = new CalculatorMenu();
            menu.ShowMenu();
        }
    }
}